# Contracts

Shared contracts live here so the web application, ingestion worker, and AI service can agree on normalized document and retrieval payloads.

Initial candidates:

- normalized source document record
- normalized ACL principal record
- chat request / response envelope
- retrieval result with citation metadata
